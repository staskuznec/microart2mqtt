# Топики MQTT

Агент публикует **всё**, что отдаёт «Малина», — топики создаются автоматически
из полей API, вручную ничего описывать не нужно.

Ниже полный справочник: как устроены имена, что в значениях и что означает
каждое поле.

## Корень

Всё висит под корнем из настроек — по умолчанию `microart/inv1`. У каждого
инвертора он свой (`inv1`, `inv2`, `inv3`), чтобы топики не пересекались.
Дальше в примерах корень — `microart/inv1`.

## Структура

```
microart/inv1/availability            online | offline
microart/inv1/map                     {…весь JSON раздела…}
microart/inv1/map/<поле>              состояние МАП
microart/inv1/bat                     [{…},{…}]  (весь JSON)
microart/inv1/bat/0/<поле>            батарейный монитор, минутные данные
microart/inv1/bat/1/<поле>            батарейный монитор, секундные данные
microart/inv1/mppt                    [{…},…]  (весь JSON)
microart/inv1/mppt/<n>/<поле>         контроллер КЭС №n (с 0)
```

Разделы соответствуют запросам API «Малины» (`read_json.php?device=…`):
`map` — один объект, `bat` — массив из двух (отсюда индексы `0`/`1`),
`mppt` — массив контроллеров (`0..3`).

Кроме отдельных значений публикуется и **весь JSON раздела** в `…/map`,
`…/bat`, `…/mppt` — для тех, кто хочет разобрать ответ целиком.

## Значения

- Публикуются **строкой, как отдаёт устройство**: `"226"`, `"50.10"`, `"-0.0"`.
  Тип не навязывается — подписчик приводит сам (в Home Assistant это
  `value_template`).
- **retain** включён: подписчик, подключившийся позже, сразу получает последние
  значения, а не ждёт следующего опроса.
- В брокер уходит только **изменившееся** значение. Раз в `republish_sec`
  (по умолчанию 10 минут) публикуется весь набор заново — на случай, если
  подписчик или брокер потеряли состояние. Поэтому редкие величины (заряд АКБ,
  суточные счётчики) подолгу молчат — это нормально.
- **QoS** — из настроек (по умолчанию 0).

## Доступность

`microart/inv1/availability` = `online`, пока «Малина» отвечает и данные идут;
`offline` — если она замолчала, агент выключили в вебе или он штатно остановлен.
`offline` также стоит завещанием (LWT): при внезапной пропаже связи его
разошлёт сам брокер.

---

# Справочник полей

Набор зависит от модели МАП, прошивки и того, что подключено: полей может быть
меньше или больше. Что реально отдаёт **ваш** инвертор — видно в топике
`microart/inv1/map` (и `…/bat`, `…/mppt`), там весь JSON целиком.

## map — состояние МАП

### Сеть на входе

| Поле | Что это |
|---|---|
| `_UNET` | Напряжение сети (вход МАП), В |
| `_INET` | Ток по входу МАП, А (грубое значение) |
| `_INET_16_4` | Ток по входу МАП, А (точное значение) |
| `_PNET` | Мощность по входу МАП, ВА |
| `_PNET_calc` | Расчётная мощность по входу МАП, ВА |
| `_TFNET` | Частота сети по входу, Гц |
| `_UNET_Limit` | Порог напряжения сети |
| `_TFNET_Limit` | Порог частоты сети |
| `_Inet_flag` | Флаг сети (см. протокол) |
| `_flagUnet2` | Флаг напряжения сети 2 |
| `_F_Net_Over` | Флаг перегрузки по сети 220 В |

### Выход и нагрузка

| Поле | Что это |
|---|---|
| `_UOUTmed` | Усреднённое напряжение на выходе МАП, В |
| `_ThFMAP` | Частота по выходу МАП, Гц |
| `_PLoad` | Мощность по АКБ, Вт |
| `_PLoad_calc` | Расчётная мощность по АКБ, Вт |

### Батарея

| Поле | Что это |
|---|---|
| `_Uacc` | Напряжение АКБ, В |
| `_Iacc` | Ток АКБ, А |
| `_I_acc_avg` | Средний ток по АКБ, А |
| `_IAcc_med_A_u16` | Усреднённый ток АКБ, А |
| `_Uch_T` | Напряжение окончания заряда с коррекцией по температуре, В |
| `_Ubuf_T` | Буферное напряжение с коррекцией по температуре, В |
| `_F_Acc_Over` | Флаг перегрузки по АКБ |
| `_fl_UAccChBUF_24h` | Флаг напряжения АКБ за 24 ч |

### Три фазы

| Поле | Что это |
|---|---|
| `_I_acc_3ph` | Общий ток потребления/заряда по АКБ, 3-фазная система, А |
| `_I_ph1`, `_I_ph2`, `_I_ph3` | Ток по фазам 1–3, А |
| `_P_acc_3ph` | Мощность по АКБ, 3-фазная система, Вт |
| `_P_ph1`, `_P_ph2`, `_P_ph3` | Мощность по фазам 1–3, Вт |

### Солнце и ветер

| Поле | Что это |
|---|---|
| `_I_mppt_avg` | Средний ток от всех MPPT-контроллеров, А |
| `_P_mppt_avg` | Средняя мощность от MPPT-контроллеров, Вт |
| `_MPPTs_mode` | Режим работы MPPT-контроллеров |
| `_I_MPPT_WIND` | Ток MPPT ветрогенератора, А |
| `_P_MPPT_WIND` | Мощность MPPT ветрогенератора, Вт |

### Счётчики энергии

| Поле | Что это |
|---|---|
| `_E_NET` | Энергия сети |
| `_E_NET_SIGN` | Счётчик направления тока энергии по входу МАП |
| `_E_ACC` | Энергия АКБ |
| `_E_ACC_CHARGE` | Энергия заряда АКБ |
| `_E_NET_B`, `_E_NET_SIGN_B`, `_E_ACC_B`, `_E_ACC_CHARGE_B` | те же счётчики, резервные копии |

### Температуры и охлаждение

| Поле | Что это |
|---|---|
| `_Temp_Grad0` | Температура внешнего датчика (на АКБ), °C |
| `_Temp_Grad1` | Температура датчика тора (DOMINATOR), °C |
| `_Temp_Grad2` | Температура транзисторов, °C |
| `Temp_off` | Порог отключения по температуре |
| `_CoolerSpeed` | Скорость вентилятора |

### Режим, статус, реле

| Поле | Что это |
|---|---|
| `_MODE` | Режим работы МАП |
| `_Status_Char` | Статус заряда (по протоколу МикроАрт) |
| `_Flag_ECO` | Флаг режима ECO |
| `_Relay1`, `_Relay2` | Состояние реле 1 и 2 |

### Ошибки и предупреждения

| Поле | Что это |
|---|---|
| `_RSErrSis` | Ошибки системы |
| `_RSErrJob` | Ошибки работы |
| `_RSErrJobM` | Ошибки задания режима |
| `_RSErrDop` | Дополнительные ошибки |
| `_RSWarning` | Предупреждения |
| `_I2C_Err` | Ошибки по шине I²C |

### Служебное

| Поле | Что это |
|---|---|
| `timestamp` | Unix-время устройства (сек) |
| `time` | Время устройства, `ЧЧ:ММ:СС` |
| `_UID` | Уникальный ID устройства (EEPROM) |
| `fw` | Версия прошивки МАП |

## bat/0 — батарейный монитор, минутные данные

| Поле | Что это |
|---|---|
| `C_100_remain` | Остаточный уровень заряда, % |
| `C_Ah_remain` | Остаточный заряд АКБ, А·ч |
| `Uacc_avg` | Усреднённое напряжение АКБ за минуту, В |
| `Iavg` | Усреднённый ток, А |
| `C20` | Пересчитанная ёмкость АКБ в C20, А·ч |
| `C20_t` | То же с коррекцией по температуре, А·ч |
| `C(I)` | Ёмкость в А·ч как функция разрядного тока |
| `TTG` | Время до полного разряда, сек (0 — разрядного тока нет) |
| `est_soc` | Оценочный уровень заряда при покое АКБ, % (−1 — оценки нет) |
| `cons_Ah` | Потреблено электричества, А·ч |
| `low_V` | Минимальное напряжение за сутки, В |
| `hi_V` | Максимальное напряжение за сутки, В |
| `cycles` | Циклов АКБ за сутки (ниже 90 %) |
| `LD` | Последний разряд, % |
| `DD` | Наибольший разряд, % |
| `AD` | Средний разряд, % |
| `Esum_from_bat` | Суммарная энергия на батарею, Вт·ч |
| `Esum_to_bat` | Суммарная энергия от батареи, Вт·ч |
| `No_async` | Число автосинхронизаций на 100 % по заряду |
| `mppt_day_E` | Энергия за день от MPPT, Вт·ч |
| `E_wind_day` | Энергия за сутки по ветру, Вт·ч |
| `time`, `timestamp` | Время батарейного монитора |

## bat/1 — батарейный монитор, секундные данные

| Поле | Что это |
|---|---|
| `Isumm` | Мгновенная сумма токов по АКБ, А |
| `dCdt` | Дельта уровня заряда, А·ч/сек |
| `dEdt` | Дельта по энергии, Вт·ч/сек |
| `IntegralC_0_t` | Интегральное приращение заряда, А·ч |
| `Integral_E_0_t` | Интегральное приращение энергии, Вт·ч |
| `t_bat` | Температура батареи, °C |
| `Sun_I` | Мгновенный ток от контроллеров, А |
| `Sun_P` | Мгновенная мощность от контроллеров, Вт |
| `wind_I` | Мгновенный ток по ветру, А |
| `wind_P` | Мгновенная мощность по ветру, Вт |
| `Ah_user` | Счётчик пользователя, А·ч |
| `Wh_user` | Счётчик пользователя, Вт·ч |
| `time`, `timestamp` | Время батарейного монитора |

Часть этих же величин (`Isumm`, `t_bat`, `Sun_P`, `dCdt` и подобные) прошивка
кладёт и в `bat/0`. Брать лучше из `bat/1`: он обновляется раз в секунду,
`bat/0` — раз в минуту.

## mppt/&lt;n&gt; — контроллеры КЭС

Публикуются, только если контроллеры подключены.

| Поле | Что это |
|---|---|
| `Vc_PV` | Напряжение панелей, В |
| `Ic_PV` | Ток панелей, А |
| `P_PV` | Мощность с панелей, Вт |
| `V_Bat` | Напряжение батареи по данным контроллера, В |
| `P_Out` | Мощность по выходу контроллера, Вт |
| `P_curr` | Мощность на АКБ, Вт |
| `I_Ch` | Ток на АКБ, А |
| `Pwr_kW` | Выработка за день от панелей, кВт·ч |
| `Temp_Int` | Температура транзисторов, °C |
| `Temp_Bat` | Температура батареи, °C |
| `I_EXTS0`, `I_EXTS1` | Ток по внешним датчикам Холла 0 и 1, А |
| `P_EXTS0`, `P_EXTS1` | Мощность по внешним датчикам Холла 0 и 1, Вт |
| `Sign_C0`, `Sign_C1` | Знак по внешним датчикам Холла |
| `Mode`, `Sign` | Режим работы контроллера и его знак |
| `MPP` | Тип MPPT (`L` — линейный, прочие — см. инструкцию) |
| `windspeed` | Частота вращения ротора ВГ (65535 — нет данных) |
| `R1`, `R2`, `R3`, `Relay_C` | Состояния реле |
| `RSErrSis` | Ошибки системы контроллера |
| `UID`, `MID` | Идентификатор контроллера и его адрес на шине |
| `T` | Тип: 0 — солнце, 1 — ветер |
| `FW` | Версия прошивки контроллера |
| `time`, `timestamp` | Время контроллера |

## Ячейки BMS

Если подключён BMS, ячейки приходят в разделе `map` массивом `bms_cells`:
`map/bms_cells/<n>/CID` (ID ячейки), `V` (напряжение, В), `I` (ток
балансировки, А), `t` (температура, °C; 127 — датчика нет).

---

## Как часто что меняется

| | Обновление |
|---|---|
| `map`, `bat/1` (секундные) | около секунды |
| `bat/0` (минутные) | раз в минуту |
| Суточные счётчики (`low_V`, `hi_V`, `cycles`, `LD`/`DD`/`AD`) | раз в сутки |

Интервал опроса задаётся в вебе агента (по умолчанию 5 с, минимум 1). Чаще
смысла нет: сама «Малина» усредняет данные с шагом 5 секунд.

Поля `time` и `timestamp` меняются каждый цикл, поэтому публикуются постоянно —
это самые «болтливые» топики. Не нужны — просто не подписывайтесь на них.

---

## Как посмотреть, что публикуется

```sh
# всё дерево инвертора
mosquitto_sub -h <брокер> -v -t 'microart/inv1/#'

# только доступность и заряд
mosquitto_sub -h <брокер> -v -t 'microart/inv1/availability' \
                             -t 'microart/inv1/bat/0/C_100_remain'

# все инверторы разом
mosquitto_sub -h <брокер> -v -t 'microart/#'
```

## Пример для Home Assistant

```yaml
mqtt:
  sensor:
    - name: "Инвертор 1 — заряд АКБ"
      state_topic: "microart/inv1/bat/0/C_100_remain"
      unit_of_measurement: "%"
      device_class: battery
      availability_topic: "microart/inv1/availability"

    - name: "Инвертор 1 — напряжение сети"
      state_topic: "microart/inv1/map/_UNET"
      unit_of_measurement: "V"
      device_class: voltage
      availability_topic: "microart/inv1/availability"

    - name: "Инвертор 1 — мощность по сети"
      state_topic: "microart/inv1/map/_PNET"
      unit_of_measurement: "VA"
      device_class: apparent_power
      availability_topic: "microart/inv1/availability"

    - name: "Инвертор 1 — солнце сейчас"
      state_topic: "microart/inv1/bat/1/Sun_P"
      unit_of_measurement: "W"
      device_class: power
      availability_topic: "microart/inv1/availability"
```
